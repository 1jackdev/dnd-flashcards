info:
  name: deck progress
  type: http
  seq: 13

http:
  method: GET
  url: http://localhost:3000/decks/{{deckId}}/progress
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
