info:
  name: delete flashcard
  type: http
  seq: 10

http:
  method: DELETE
  url: http://localhost:3000/flashcards/{{flashcardId}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
