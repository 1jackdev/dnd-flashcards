info:
  name: list flashcards
  type: http
  seq: 7

http:
  method: GET
  url: http://localhost:3000/decks/{{deckId}}/cards
  auth: inherit

runtime:
  variables:
    - name: deckId
      value: 8086ba10-ddc5-4e32-b952-cc3ae72951ed

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
