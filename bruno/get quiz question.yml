info:
  name: get quiz question
  type: http
  seq: 17

http:
  method: GET
  url: http://localhost:3000/quiz/{{quizQuestionId}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
