info:
  name: get due cards
  type: http
  seq: 11

http:
  method: GET
  url: http://localhost:3000/decks/{{deckId}}/due
  headers:
    - name: Authorization
      value: Bearer {{token}}
      enabled: true
  auth: inherit

runtime:
  variables:
    - name: token
      value: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
