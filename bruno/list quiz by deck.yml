info:
  name: list quiz by deck
  type: http
  seq: 16

http:
  method: GET
  url: http://localhost:3000/decks/{{deckId}}/quiz
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
