info:
  name: list quiz by tag
  type: http
  seq: 26

http:
  method: GET
  url: http://localhost:3000/tags/quiz?tags={{tag}}
  auth: inherit

runtime:
  variables:
    - name: tag
      value: condition

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
